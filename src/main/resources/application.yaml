spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shop_database
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect


  security:
    jwt:
      token:
        secret-key: bezKoderSecretKey
        expire-length: 86400000
